# front deployment
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecommfront
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ecommappfront
  template:
    metadata:
      labels:
        app: ecommappfront
    spec:
      imagePullSecrets:
        - name: ecr-registry-key
      containers:
        - name: frontcontainer
          image: 314146337686.dkr.ecr.us-east-1.amazonaws.com/ecomm.ci.cd.front:0.0.2-125
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          env:
            - name: BACKEND_HOST
              value: "ecommbacksvc"
            - name: BACKEND_PORT
              value: "8080"
---
apiVersion: v1
kind: Service
metadata:
  name: ecommfrontsvc
spec:
  selector:
    app: ecommappfront
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
